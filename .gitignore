# Dependencies
node_modules/

# Build / dist output
dist/
build/
out/
*.tsbuildinfo

# Env & secrets (Claude key, etc.) — never commit
.env
.env.*
!.env.example

# Local database
*.sqlite
*.sqlite-journal
*.db

# Brainstorm visual-companion artifacts (mockups, server state)
.superpowers/

# Logs
*.log

# OS / editor cruft
.DS_Store
Thumbs.db

# AURA brain runtime dir — contains auth token (config.json) and local SQLite DB; never commit
.aura/

# WXT generated dir + build output
.wxt/
.output/

# Playwright MCP scratch (snapshots/screenshots)
.playwright-mcp/
